.factorio-data/
# write-data for the throwaway test instance (dev/server-test.sh)
.factorio-test/
mods/*.zip
*.log
__pycache__/
.scratch/
# Vendored API docs, pinned to the server build (2.0.77); re-download per version, don't commit.
doc-html/
