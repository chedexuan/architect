; Clone of dev/config.ini for the throwaway test instance. Only write-data differs, and it has to:
; two servers sharing a write-data directory fight over _autosaveN.zip and factorio-current.log.
[path]
read-data=__PATH__executable__/../../data
write-data=/home/admin/factori/.factorio-test

[general]
locale=en
